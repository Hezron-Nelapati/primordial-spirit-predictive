services:
  spse:
    build: .
    ports:
      - "5001:5000"
    volumes:
      # Persist graph.db and corpus files between container restarts
      - ./data:/app/data
    environment:
      PORT: "5000"
    restart: unless-stopped
