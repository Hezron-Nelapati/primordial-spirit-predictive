sentence-transformers==5.3.0
scikit-learn==1.8.0
numpy==2.4.3
nltk==3.9.3
datasets==4.8.2
transformers==5.3.0
spacy==3.8.11
flask==3.1.3
# torch is installed separately in the Dockerfile with the correct wheel
# index for the target GPU (CUDA / ROCm / CPU).
# For local non-Docker runs: pip install torch (picks up the right build automatically).
